export enum GoalStatus {
  VALID = "valid",
  CANCELLED = "cancelled",
  PENDING = "pending",
  MISSED = "missed",
  SAVED = "saved",
}

export enum GoalType {
  NORMAL = "normal",
  PENALTY = "penalty",
  OWN_GOAL = "own_goal",
  PENALTY_SHOOTOUT = "penalty_shootout",
}
